# v0.30 密码恢复验收

**简体中文** · [English](en/VERIFICATION-v30.md)

本阶段完成 Workers/D1 内的离线密钥密码找回及密钥写入撤权校验，保持无容器架构。使用流程、第二因子要求和剩余恢复能力见 [密码恢复说明](PASSWORD-RECOVERY-v30.md)。完整 GitLab/Gogs 目标继续推进。

## 测试与并发证据

类型检查及 320 项测试通过。新增 8 项测试覆盖密钥摘要与版本、轮换/撤销、到期与停用、管理员撤销、仅统一登录账户限制、跨域与 PAT 拒绝、一次性并发消费、旧会话/PAT/JWT 公钥撤销、OIDC 待办流程删除、签名公钥和关联身份保留、MFA 验证及防重放、审计失败事务回滚、相同匿名错误和速率限制。

额外故障注入复现了既有身份密钥接口的在途写入缺口：先完成会话读取，在实际公钥 INSERT 前撤销会话，原实现仍返回 201 并留下一个密钥。修复后同一注入返回 403，密钥数量为零。单元测试同时覆盖 JWT 委托公钥与真实 SSH Ed25519 签名公钥的创建/删除、会话删除/过期和用户停用；正常的密钥注册仍然成功。

这证明所测写入路径重新验证了当前会话，不代表所有已授权的在途业务操作都会因密码恢复自动回滚。

## 本地与 Cloudflare 浏览器验收

最终运行时提交为 `217c8cbb7030044cfc9dce1b2d84729007fa360b`，主 Worker 候选版本 `806f75ab-893b-4ece-8b8a-a15c5ee683e5`。本地 42 次、生产 41 次显式响应检查通过（不包含页面后台请求及清理查询）：

- 新建独立测试账户及私有项目，写入真实 Git 提交，注册 PAT 和可实际验证的签名 JWT。
- 通过桌面账户安全页生成、下载、轮换和撤销密码恢复密钥；下载内容在测试内核对后删除，没有写入报告。
- 从登录页“忘记密码？”进入手机恢复页面，完成未启用 MFA 的密码找回；旧会话、PAT 和实际 JWT 均被拒绝，重新登录后 Git 文件字节不变。
- 启用 TOTP 后，之前生成的密码恢复密钥失效；新的密码恢复密钥缺少第二因子时被拒绝，配合未用过的 MFA 恢复码才能找回密码。找回后 MFA 保持启用，正常登录仍要求第二因子，私有 Git 内容不变。
- 检查两次恢复的审计历史、密钥消费状态和管理员撤销会话时的恢复密钥清除。
- Chromium 桌面 `1440×1000`、手机 `390×844`，生成、保存、找回表单和成功页面可操作，无脚本异常；手机表单及成功页无横向溢出。截图已检查，密钥区域遮挡，没有记录密钥、密码、验证码或 Cookie。

生产最终测试账户 `recovery_v30_8bfc651b`，项目 `5f46ec89-24f2-43a2-b045-d6958e40931e`；本地 `recovery_v30_503d9dc6`，项目 `574f63f4-eaef-428f-ba18-d6fcf3ef2bbe`。完整验收及清理进程均退出 0，独立 D1 查询确认测试项目、登录凭据、密码恢复密钥和仍启用的测试用户数量为零。测试用户以停用状态保留审计历史。

本地首轮因测试 JWT 缺少接口要求的 `typ: JWT` 头而失败，清理完成后修正并重新通过；随后依据截图修正了生成密钥后的状态文字。第一次生产验收通过后又发现上述公钥写入竞争，因此再次部署，并在本地和生产完整复验最终运行时，没有把早期验收当成最终版本的替代证据。

## 数据与发布

生产 D1 升级前导出到本机私有文件，在独立 SQLite 数据库恢复后应用 `0024_password_recovery.sql`。67 张既有表行数保持不变，406 条既有审计记录保留，完整性检查通过，外键违规为零。此处只验证 D1 备份恢复，不是完整 R2/DO 灾难恢复演练。随后本地和生产迁移成功。

生产构建通过。本阶段仅部署主 Worker，编译 Worker、应用网关及 旧站 路由没有修改。最后补充的文档与 OIDC 清理断言不改变已完成两地验收的运行时代码。最终发布使用源码白名单归档及 `scripts/verify-release.mjs` 核对线上资源、OpenAPI、版本、源代码和 旧站；源码实际推送后另以远端引用、唯一 Git 审计及独立镜像严格 fsck 核实。

邮件验证与恢复、全部第二因子丢失后的独立身份核验、Access/普通 OAuth、合并队列及其余完整平台差距仍在持续目标中。
