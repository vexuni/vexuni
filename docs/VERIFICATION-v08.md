# v0.8 验证记录

**简体中文** · [English](en/VERIFICATION-v08.md)

2026-09-08，Cloudflare 原生 CODEOWNERS、合并关闭 Issue、R2 并发 I/O。

- TypeScript 检查通过，107 个单元测试通过。新增验证规则优先级、目标快照、防自批准、空间/角色继承、停用和撤权、陈旧批准、删除与文件/目录替换、Issue 指令解析、跨项目过滤、D1 事务故障恢复、人工重开后幂等性。
- R2 测试检查读取去重、最多双路并发、错误时排空在途操作、引用不发布、图类型校验和 32 MiB 预算；原有不可变对象冲突检测回归保留。
- 本地新增 HTTP 验收 54 项通过：独立审批、撤销成员后门禁变化、源码修改不能修改本次审批规则、默认分支关闭、无变化合并、非默认目标不关闭、重复合并后保留人工重开、MR 描述变更后拒绝旧 revision 合并。
- 本地跨 Fork 63 项 HTTP 检查及原生 Git clone/fsck 通过；协作 81 项、基础 API 43 项及 Git/LFS/并发回归通过。51 项行为对照检查继续通过。
- 生产新增 HTTP 验收 53 项通过（本地额外一次登录请求）；CODEOWNERS、Issue 联动、旧 revision 拒绝、无变化合并和幂等重试均在真实 D1/R2/DO 上验证。生产跨 Fork 本次 61 项 HTTP 检查及真实 Git clone/fsck 通过，确认未发布的私有分支对象不进入目标仓库。条件竞争路径可能使请求计数在各轮略有不同。
- 生产 CODEOWNERS 临时空间 `owners_v08_73553bba` 已清理，测试账号停用且凭证撤销；跨 Fork 临时空间 `review_v07_58fbb4e8` 也已清理，测试账号停用且凭证撤销。
- D1 增量迁移 0009 已在线成功应用，迁移前导出备份已保存在本机受限文件中。主站健康为 v0.8.0，HTML 及六个关键资源与本地构建逐字节一致；example.com 返回 200 且仍为 旧站。
- Mac 锁定，v0.7/v0.8 浏览器交互验收仍待用户解锁后完成。纯 UI 渲染测试不能替代实际点击验证。

完整 GitLab/Gogs 目标保持进行中，差距见 [持续能力矩阵](CLOUD-NATIVE-v05.md)。
