# v0.34 代码索引复用验收

**简体中文** · [English](en/VERIFICATION-v34.md)

本阶段完成同仓库 Git blob 内容复用，完整 Cloudflare 原生 GitLab/Gogs 目标继续有效。

类型检查和 354 项测试通过。新增检查覆盖未变化/重命名/复制文件复用、实际新增倒排行、无引用内容回收、手动新周期、构建中的再次重建、开始构建前的并发请求，以及旧 Worker 消费迁移请求后的版本补建。现有实时权限、分页、Unicode、丢失 D1 响应恢复和默认分支条件更新检查通过。

本地原生 Git 与浏览器验收通过 31 次响应/场景断言。含重命名、删除、复制和新增文件的推送复用 19 个文件，新建 1 份内容，仅写入 42 条倒排关系，对应 919 条按路径计数的逻辑关系。手动重建创建全新内容 ID；复用计数显示、固定提交跳转、成员撤权、手机布局及原生 Git 完整性检查通过。实际 D1 查询确认夹具仓库、凭据、启用账户、文档、索引状态、内容和捕获的内容倒排关系均为零。

迁移前生产 D1 导出为 112,186,216 字节，在隔离 SQLite 副本恢复：原 72 张表计数保留，468 个旧文档及 800,638 条旧倒排关系保留，迁移前后完整性正常、外键错误为零。此备份不是 R2/DO 完整灾备。查询计划验证新旧格式均从 gram 主键定位，新格式通过内容/代号/仓库路径索引连接；这不是大规模云端延迟基准。

生产迁移 0027 成功，候选 Worker `6674ac05-8f67-477a-82be-7706acf1bc92`（提交 `c5d8652`）通过 40 次响应/场景断言，浏览器异常为零。重命名/复制/新增推送同样复用 19 个文件、只写入 42 条新倒排关系；实际 D1 查询确认夹具仓库、凭据、启用账户、文档、索引状态、内容及捕获的倒排关系均为零，桌面复用统计截图检查通过。19 个线上文件与候选发布包逐字节匹配，health 0.34.0、API/Git 鉴权响应及 旧站 检查通过。

源码仓库迁移排队期间，旧提交 `231efa49` 的代码仍可检索。随后通过维护者重建接口请求首次格式转换，最终源码推送将验证普通更新的内容复用；自动 cron 补建不包含在这项云端转换证明内。最终文档/API 描述更新不改变已验收的运行时代码。
