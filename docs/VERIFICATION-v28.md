# v0.28 跨项目协作搜索验收

**简体中文** · [English](en/VERIFICATION-v28.md)

本阶段增加 Workers/D1 的统一项目、Issue、合并请求和当前 Wiki 搜索；不是完整 GitLab/Gogs 目标的完成声明。功能契约和边界见 [SEARCH-v28](SEARCH-v28.md)。

## 自动检查

- `npm run check`：类型检查和 305 项单元测试通过，其中新增 8 项搜索测试。覆盖直接/继承权限、个人所有权、管理员不绕过私有项目、凭据到期/撤销/禁用、当前可见性/归档/转移/删除、字面中文和标点、正文片段、当前 Wiki、状态筛选及错误凭据类型。
- 151 个私有项目的分页测试逐页取齐且无重复，匿名读不到其内容；旧分页游标不能保留已撤销权限，不能用于另一用户或另一组筛选。按键翻页不因前序记录删除而产生 offset 位移。
- `npm run build:production` 通过。没有数据库迁移、FTS 虚拟表或额外 Cloudflare 资源；主库迁移版本保持 `0022_ci_private_packages.sql`。

## 本地真实运行时

`npm run test:search` 在本地 Wrangler Workers、D1、R2 和 Durable Objects 中完成，退出码 0。夹具含 3 个项目、3 条 Issue、1 条 MR 和 1 个 Wiki；通过提交文件/分支 API 创建实际 Git 对象及 MR。

51 次 API 响应检查（不含清理），含 22 次成功搜索。验证公开/私有隔离、直接与继承成员、只读 PAT、撤销、旧游标、状态/归档、中文和字面 `%_\\`、SQL 引号、长正文及 Wiki 修改即时生效。

Headless Chromium 完成 1440×1000 桌面和 390×844 手机页面、刷新、查询、结果跳转到具体 MR、浏览器返回、正文转义和命中高亮；无页面脚本异常或手机横向溢出。两张完整页面截图已目视检查。仅拥有项目权限时，空间选择器正确显示当前筛选空间。

最初浏览器验收因脚本未包含 MR 标题中的 `!编号` 而失败，已完成清理并修正选择器，随后完整重跑通过。

## 真实 Cloudflare 部署

功能验收提交：`2b32737f63fc117bd07343a5e377b02a0cba5717`。
验收主 Worker 版本：`d7ceaee2-9bf9-4d2e-b3a2-b54347ab6605`。

在 `https://git.example.com` 完成同一组 50 次 API 响应检查（不含清理），含 22 次成功搜索；这轮线上不重复浏览器操作。相同私有权限、撤销、归档、Wiki 更新和真实 Git/MR 数据路径通过。主 Worker 是本轮唯一部署的服务。

本机测得该小夹具本地搜索中位 4 ms、最大 5 ms；线上端到端中位 539 ms、最大 612 ms。这是包含客户端网络的样本，不是 Worker CPU 时间或大规模负载基准；查询仍需扫描可访问的协作内容，未宣称全文索引或全局代码索引性能。

本地与生产验收均已完成后台回收并退出 0。独立读取实际 D1 结果确认临时工作空间、项目和凭据均为 0；生产还核对 Issue/MR 为 0。临时用户保留禁用记录，其凭据已撤销，原用户 PAT 保留。本轮无数据库迁移，不需要更改现有备份结构。

最后补全验收文档并修正清理日志文案，未改变已验收的运行时代码。源码通过 `npm run source` 明确白名单打包，线上静态文件、OpenAPI、源代码包和健康版本使用 `scripts/verify-release.mjs` 对照本地构建验证。

## 后续

全局代码索引、评论/讨论搜索、相关度排序、合并队列及其余身份/包格式/规模功能仍在总目标内。本次功能保留原有项目内代码搜索与源代码公开机制。
