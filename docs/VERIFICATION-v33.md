# v0.33 OAuth 验收记录

**简体中文** · [English](en/VERIFICATION-v33.md)

本阶段添加 GitHub 与 GitLab OAuth 登录适配，完整 GitLab/Gogs 平台目标继续有效。

类型检查及 349 项测试通过。新增检查覆盖固定 HTTPS endpoint/可信主机、PKCE 参数、稳定数字 ID、token/admin 字段不返回、GitHub 已验证邮箱与有界分页、GitLab 活跃账户和已确认主邮箱、畸形 token/响应/重定向/超限 JSON，以及账户关联后改名仍登录同一用户、提供方停用和登录中配置变更使流程失效。原有 OIDC 测试也通过。

本地 Cloudflare 浏览器验收通过 38 次响应及场景断言。验证管理员界面协议选择/配置、外部账户显式关联、稳定 ID 登录、本地 MFA（错误码拒绝与恢复码完成）、提供方停用后的会话/PAT 撤销、普通无密码注册、最后一种登录方式保护、设置本地密码及手机布局。D1 实际查询确认夹具身份、凭据、启用或仍为管理员的夹具账户、提供方与流程均为零；禁用账户和审计历史保留。

浏览器验收使用独立临时 Worker/DO 提供方。它实现 GitLab OAuth 协议，用随机独立凭据和一次性代码测试真实网络路径；不是对商业 GitHub/GitLab 应用的实测声明。生产候选 `08c5d443-ab4e-48ec-bbfe-14dc57d117fd`（代码提交 `9067390`）通过 37 次响应及场景断言，覆盖相同完整流程，浏览器异常为零。生产夹具提供方 `7e7f1beb-66b2-4cbf-aaaf-842713265436`、身份和流程均已删除，夹具账户停用且不再是管理员，凭据计数为零。桌面管理截图及本地手机账户页检查通过，生产手机页通过无横向溢出断言并留存截图。

临时测试 Worker `vexuni-oauth-proof-f426839e` 在本地及生产验收、清理全部结束后删除。最终文档版本与通过生产验收的候选运行时代码一致，不需要 D1 迁移，schema 仍为 0026。候选 19 个线上资源/源码文件逐字节匹配，health 0.33.0、API/Git 鉴权响应及 example.com 旧站 检查通过。随后发布验收文档并同步源码；实际提供方配置仍由实例管理员负责。
