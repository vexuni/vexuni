# v0.24 空间 CI 变量验收

**简体中文** · [English](en/VERIFICATION-v24.md)

日期：2026-09-09（Asia/Singapore）。契约与限制见 [空间变量](CI-WORKSPACE-VARIABLES-v24.md)。生产验收候选提交 `e4df35c`，主 Worker 版本 `0c4675b2-8514-45fb-ad09-81f0b27f974c`；之后仅补充此文档和对应源码归档，运行时代码一致。

| 环境         | 检查                                 | 结果                                                                      |
| ------------ | ------------------------------------ | ------------------------------------------------------------------------- |
| 类型与单元   | `npm run check`                      | 类型检查及 264 项单元测试通过，含 8 项空间变量测试                        |
| 生产打包     | `npm run build:production`           | Wrangler dry-run 通过，既有绑定保留                                       |
| 本地共享变量 | `npm run test:workspace-variables`   | 49 项检查、88 次请求通过，真实隔离 Worker 与发货版外部 Runner             |
| 本地项目变量 | `npm run test:variables`             | 41 项检查、64 次请求通过                                                  |
| 空间变量界面 | `npm run test:workspace-variable-ui` | 19 项检查通过：空间入口、CRUD、只写值、所有者管理、项目只读继承及手机布局 |
| 项目变量界面 | `npm run test:variable-ui`           | 原有 16 项检查通过                                                        |
| 本地核心 Git | `npm run test:e2e`                   | 43 次 API 断言及原生 Git、并发、LFS、对象持久化检查通过                   |
| 生产共享变量 | 显式远程 opt-in 与私有令牌文件       | 49 项检查、111 次请求通过                                                 |
| 生产项目变量 | 原有变量回归                         | 41 项检查、81 次请求通过                                                  |
| 生产 Git/CI  | `npm run test:workflow-git`          | 28 次检查及 push → 版本化 DAG → clone/严格 fsck 通过                      |

轮询次数会随队列延迟变化；请求数与场景数不是同一个计数。生产共享变量验收包含项目页面元数据分离、项目通用值覆盖空间精确环境、暂停项目定义阻止回退、删除覆盖恢复继承、兄弟项目实际使用同一共享定义、Worker 和外部 Runner 消费输入、不同编码/异常/分块日志脱敏、租约拒绝、轮换取消及历史脱敏、MR 及其重试无法获得密钥。

新增 SQLite 单元测试验证：独立空间加密作用域、跨项目消费和历史脱敏；覆盖优先级及暂停规则；空间所有者降级/移除/禁用后取消各项目已绑定的任务，即使保留项目维护者角色也不能继续授权；项目转移后不能借其他项目的授权使用旧快照；选择期间出现覆盖、转移或撤权时原子拒绝；MR、分支保护和引用限制；仅空间所有者与有效写凭据可管理、版本冲突及暂停接管；已完成子任务使用的共享定义轮换后取消仍在执行的父工作流与兄弟任务。

迁移前导出生产 D1，在独立 SQLite 副本执行 `0019_workspace_ci_variables.sql`，59 张既有表行数保持一致，完整性及外键检查通过；随后生产迁移成功。此备份验证仅覆盖 D1，不代表 R2/DO 全站恢复。原项目变量密文格式、Git 对象和原存储身份不变；编译 Worker 与应用网关未随本次重新部署。

验收程序结束时删除临时项目和空间、撤销 Runner，并停用浏览器测试成员。源码在发布前检查不含生产 PAT，私有备份和验收报告不进入源码归档。最终使用 `npm run verify:release` 核对线上模块、OpenAPI、源码归档和健康版本，验证 旧站，再从独立镜像 fetch 并严格 fsck 对照发布提交。

本次共享值为专用随机测试值，没有把真实 Cloudflare 部署令牌交给测试 Runner。Cloudflare 发布能力沿用已验收的构建和应用发布实现；这里验证的是共享值的传递、限制与撤销，不额外声明用真实账户密钥执行了一次外部 Wrangler 部署。嵌套组、实例变量、文件类型变量和完整 GitLab YAML 兼容仍在本次范围之外，总平台目标继续有效。
