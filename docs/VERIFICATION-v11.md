# v0.11 验证记录

**简体中文** · [English](en/VERIFICATION-v11.md)

2026-09-08，项目转移与重命名。

- TypeScript 和 134 个单元测试通过。新增覆盖跨空间继承角色变化、显式成员保留、UUID/协作/CI 历史保留、租约与连接撤销、应用关闭、目标冲突全部回滚、源/目标事务内撤权、归档状态保留、历史路径保留/回迁、同空间重命名保留连接，以及请求级 D1 包装器的 RETURNING、batch、不可变绑定、旧版本拒绝与整体回滚；额外覆盖 D1 只读快照不增加数据库写入、转移后旧请求读取拒绝、DO GET/HEAD 版本屏障、提交后缓存故障恢复及冷启动恢复。
- 本地转移验收 79 项通过：个人到团队、团队间、归档项目转移、重命名；原生 Git clone/fetch/push/fsck；LFS 保留；旧路径与 JWT、空间成员、只读 PAT 权限；真实隔离 JS 云 CI 生成部署版本。临时项目和空间已清理，测试用户停用并撤销凭据。
- 本地既有归档 63 项、Issue 工作流 62 项、协作/隔离云 CI 81 项回归通过；归档和 Issue 临时项目已清理。加入读取保护后，生产转移验收 81 项、归档回归 61 项、Issue 回归 61 项通过，包含实际云端应用转移前可访问、转移后返回 404；生产临时项目、工作空间及应用已清理，测试用户停用并撤销凭据。完整浏览器交互仍待 Mac 解锁验收。

- 读取保护部署后，13 个资源及源码归档逐文件哈希/MIME/版本缓存验证通过，健康接口 0.11.0，旧站 正常。最终发布使用 `npm run verify:release` 再次核对。转移验收计数包含 CI 状态轮询，随任务完成时序变化；此前生产轮次为 83 项。

总目标保持进行中，参见 [持续能力矩阵](CLOUD-NATIVE-v05.md)。
