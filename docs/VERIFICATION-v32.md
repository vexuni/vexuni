# v0.32 验收记录

**简体中文** · [English](en/VERIFICATION-v32.md)

本阶段加入默认分支全局代码索引，完整平台目标继续有效。实现及限制见 [代码搜索](CODE-SEARCH-v32.md)。

本地首轮实际 Workers/R2/DO 验收通过 26 次 API 响应断言（包含状态轮询），验证 19 个文本文件可索引、二进制/大文件跳过、匿名隔离、读者权限、网页重建、固定提交行跳转、原生 Git 新增/重命名/删除后索引更新、成员撤权、手机无横向溢出及无浏览器异常。原生克隆与严格 fsck 通过。清理后 D1 实际查询确认夹具项目、凭据、启用账户、索引文档和状态均为零；禁用账户和审计历史保留。

最终类型检查与 342 项测试通过。新增索引测试覆盖不可见构建代号、原子发布、旧快照一致性、删除/重命名与旧代号回收、D1 丢失响应恢复、默认分支并发变更、Unicode 字面量、实时权限与分页、跳过预算以及唤醒失败轮转；真实 Repository 类测试进一步注入默认分支 D1 投影故障，重建 DO 后通过 alarm 恢复元数据和目标分支索引。

最终本地实际验收通过 31 次 API 响应断言（包含轮询），在前述流程上增加原生推送另一个分支并切换默认分支，确认索引 SHA、分支和路径集合全部切换。一次重跑因旧验收克隆目录已存在而失败，已清理夹具并改用随机独立目录，不是 Git 服务拒绝克隆；成功重跑保留了新增恢复逻辑。

生产迁移前导出 D1，以关闭外键导入、重新启用外键的方式恢复到隔离 SQLite；原有 69 张表计数一致，迁移 0026 后完整性检查正常、外键错误为零。这只验证 D1 备份与迁移，不等同于完整 R2/DO 灾备演练。候选部署 `761394b9-cba5-4186-a4f7-ebf85acd2d23` 的生产验收通过 29 次 API 响应断言（包含轮询）：19 个文本文件、2 个明确跳过文件，原生 clone/push/rename/delete/fsck、维护者网页重建、固定提交行跳转、撤权、手机布局及默认分支切换均通过。首次索引 SHA 为 `5150a255de06dfa6aac9626e712050ec7fa3aa6d`，原生更新后为 `7418a9821a04130635e9517606b80e3adfee271a`，切换到 stable 后恢复为首次 SHA 的路径集合。桌面与手机截图均检查，浏览器异常为零。

候选线上 19 个静态/源码文件逐字节匹配、health 0.32.0、API 与 Git 鉴权响应及 example.com 旧站 检查通过。生产夹具清理完成后，D1 实际查询确认项目、凭据、启用夹具账户、文档与索引状态均为零。此次首发的运行时代码与通过验收的候选一致，随后只补充验收文档。

现有 vexuni 源码项目没有手动触发重建；定时任务自动创建状态并启动索引，已观察到 168 个文件检查、167 个文件进入待发布代号，证明历史项目补建也实际运行在 Cloudflare。这个中间计数不是完整覆盖声明；完整覆盖、跳过原因及当前提交以项目索引状态为准。最终发布资源校验与 Git 推送结果记录在发布日志，源码提交可从项目 Git 历史获取。
