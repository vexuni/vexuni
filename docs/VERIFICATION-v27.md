# v0.27 私有 npm 云构建验收

**简体中文** · [English](en/VERIFICATION-v27.md)

日期：2026-09-09（Asia/Singapore）。契约见 [私有包构建](CI-PRIVATE-PACKAGES-v27.md)。候选提交 `3b89d40f17688b1b7a81cad7872d671abdc994b5`；编译 Worker `ce9bc429-1847-4018-a751-ce334aa852c2`，主 Worker `15c05d96-4267-42bb-b132-bd6fba9dce11`。最终文档发布保持运行时代码与该候选一致。

| 检查                            | 结果                                                              |
| ------------------------------- | ----------------------------------------------------------------- |
| 类型检查与单元测试              | 297 项通过，包含 8 项私有构建专项测试                             |
| 主 Worker / 编译 Worker dry-run | 均通过；编译 Worker 压缩约 3.8 MiB，仍无账户存储绑定              |
| 本地私有构建与浏览器模板        | 30 项检查、66 次直接 API 请求通过                                 |
| 本地公开 npm 构建回归           | 23 项检查、42 次直接 API 请求通过                                 |
| 本地核心 Git                    | 43 次 API 断言及原生 Git、并发、LFS、持久化检查通过               |
| 生产私有 npm 构建               | 29 项检查、117 次直接 API 请求通过（包括状态轮询）                |
| 生产原生 Git/CI 回归            | 28 次检查通过，包括推送、固定提交 DAG、克隆、严格 fsck 和状态轮询 |

私有专项使用真实 npm CLI 发布作用域包，再生成混合私有包与 Preact 的锁文件，提交到私有项目。真实编译 Worker 完成 TSX 构建；检查固定 SHA、R2 JS 产物、动态 Worker 应用响应、两次发布、比较交换回滚与陈旧激活拒绝。另验证浏览器 JS/CSS 静态产物与应用网关响应。没有依赖外部 Runner 或容器。

失败路径覆盖私有包 SHA-512 不符、未声明 registry 项目、旧部署令牌、撤销令牌和不支持的 `node:fs` 导入；失败运行没有新产物或应用版本。轮换部署令牌后，更新显式选择的加密变量可恢复构建。Chromium 验证 CI/CD 页的私有构建模板正确选择变量及项目；桌面截图经视觉检查，本轮不额外声明手机浏览器专项覆盖。

单元测试在真实 SQLite 迁移结构上验证 R2 读取前授权、包名/版本/文件/完整性匹配、秘密不进入编译数据、编译私有包不发起外网下载、自然到期、上传途中撤权、工作流父子与已完成构建任务的依赖授权、原子产物/完成状态门禁。自然到期后，即使所有子任务刚完成，父工作流也被协调器终止。既有变量、缓存、工作流、归档与转移回归仍通过。

生产迁移前私密导出 D1，在独立 SQLite 恢复并应用 `0022_ci_private_packages.sql`；66 张既有表行数保持一致，完整性与外键检查通过。随后生产迁移成功，先部署编译 Worker，再部署主 Worker。应用网关和原有 R2/D1/DO 身份保留。此备份验证仅覆盖 D1，不等于全站 R2/DO 灾备恢复。

本地专用构建夹具已清理；只读 D1 查询确认空间、项目、包版本、上传清理表、工作流、部署与私有依赖绑定记录为零。最终私有验收的会话已主动注销，npm 临时目录已移除。核心 Git 脚本按既有行为留下 `owner/e2e_e9be8918` 审阅示例，临时令牌已撤销。

生产私有编译首次任务 `940ebba1-39e7-48ca-add1-2fd033ea8a21`，第二次 `d96ce167-8342-47f8-98d3-212fe0006b20`，完整性拒绝 `930c2bd1-6b0b-4d59-8504-e20307bdccd2`，浏览器构建 `fecf17cb-c7e0-4c82-95da-b4a03397e823`。Git/CI 工作流 `851d340b-04c3-4f8b-b64c-83a044728b1b` 成功运行提交 `d1fe8ca19dbb7978e080209a132dad7ee46a3257`。两项生产验收均已完成清理；只读查询确认临时空间、项目、包、上传清理记录、运行、部署和私有依赖绑定均为零。原始用户 PAT 保留。

最终发布使用 `npm run verify:release` 核对线上静态模块、OpenAPI、源码归档、健康版本和 旧站。源码同步后，在独立 Git 镜像执行 fetch、严格 fsck 并对照提交，记录存于发布工作区。完整 GitLab/Gogs 目标继续推进，包下载缓存、更广泛工具链、身份扩展与其余协作能力仍属后续工作。
