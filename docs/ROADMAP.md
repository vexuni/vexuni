# 能力范围与交付记录

**简体中文** · [English](en/ROADMAP.md)

本文记录 vexuni 的能力范围与交付历史，当前版本为 **v0.1.0-beta**。文中阶段编号为开发顺序标识，属于版本重置前的历史记录，与当前版本号无关。逐项能力映射保存在 [parity.json](parity.json)。

## 开发目标

1. **鉴权和引用隔离**：用户命名空间下注册/撤销 JWT 公钥；精确 scope、repo、有效期校验；首次匹配引用策略；SSH/OpenPGP 提交验签；Git Notes；临时和导入远程。
2. **完整 Git API**：分支/标签 CRUD 与游标；提交元数据/签名、按路径历史；二进制/流式提交；文本和 binary patch；恢复提交；三方合并、预览、冲突和 squash；最小行差异；原始文件条件请求、范围/HEAD；归档过滤、blame 和正则搜索。
3. **仓库生命周期**：自定义名称、修改默认分支、独立 fork、删除状态与异步对象清理、ID 到 URL 查询。
4. **上游连接**：加密保存 HTTPS 凭证、公共 GitHub 导入、普通 Git 上游同步、GitHub App 安装令牌和签名 webhook；只同步普通 heads/tags，临时状态不外传；失败可观察，成功必须有实际 Git/R2 状态。
5. **SDK 与 Agent 使用**：TypeScript/Python/Go 的对应操作、短期委托 Git URL、提交构造器；MCP 和机器可读文档；内存、会话、恢复、实时 diff、产品文件和并行尝试示例。
6. **验收与交付**：每个契约都有成功/失败测试；原生 Git 验证对象、签名、Notes 和协议；测试旧接口回归；更新部署配置/迁移/文档后再发布。

## 交付结果

六个开发目标已完成对应实现与验收。51 项均有真实后端实现和可重复验证证据，40 个首选 REST 操作已在 Cloudflare 临时仓库验收；完整结果和未覆盖事项见 [验证记录](VERIFICATION.md)。当前版本提供三种语言 SDK、七类 Agent 工作流、MCP、OpenAPI，以及 Git 工作台/密钥/上游管理页面。

真实私有 GitHub App 的安装配置仍由操作者提供；其密码学与提供方协议由可重复测试覆盖，公开 GitHub 同步已在真实 Cloudflare 上验证。规模、SLA、原生 Git 未支持的协议与复杂合并边界不包含在“功能完成”的声明中。

## 早期基线的主要差距

早期基线只有 PAT/会话鉴权、heads/tags、基本读写、快进合并、文本编辑和简单检索。上述新增能力均不能仅凭一个路由或 SDK 方法就标记完成，必须连接真实持久化后端。Issues、评论、成员管理和网页审阅属于 vexuni 的额外能力，会继续保留。

## 对齐原则

- 维持完全无容器架构，不运行服务端 Git 进程。服务域名和命名空间 API 路径采用 vexuni 自身约定。
- vexuni 的数据始终保存在 R2，不采用热盘复制/冷存储模型；导入后的持久化无需热盘迁移。
- 大仓库吞吐、TB 级容量和 SLA 不因 API 完成而自动成立。保留经过验证的应用/平台限额，单独记录规模测试边界。
- GitHub App 及私有上游需要操作者提供连接配置；实现和可重复集成测试应先完成，缺少真实外部安装信息不可以用假成功掩盖。

## 平台扩展目标

用户已指定：代码高亮、通用构建与 Cloudflare 部署、多空间、权限分配、管理员后台。验收范围、权限矩阵、Runner 配置和当前边界见 [平台说明](PLATFORM-v04.md)。功能开发、权限/协议回归与生产迁移已完成，线上核心验收通过；测试证据和 Runner 部署前提见 [阶段验证记录](VERIFICATION-v04.md)。

## Cloudflare 原生 GitLab/Gogs 持续目标

以 [能力与差距矩阵](CLOUD-NATIVE-v05.md) 为当前状态，逐步补齐跨 Fork 审阅、身份安全、Markdown/预览、完整项目协作及原生构建。阶段交付不表示总目标完成。

## 账户与展示

本轮实现见 [ACCOUNT-v06.md](ACCOUNT-v06.md)：TOTP/恢复码、会话撤销、个人资料/权限过滤活动、Markdown 与栅格图片预览。其余差距继续追踪，不以阶段发布结束总目标。

## Fork 审阅与讨论

见 [REVIEWS-v07.md](REVIEWS-v07.md)：同一 Fork 网络贡献、固定对象快照、目标 CI、行级讨论与解决门禁、DO 串行审阅变更。v0.13 已用独立无头浏览器补验真实 Fork MR 创建、分页、重新打开、CI 和合并交互，见 [GIT-PREFETCH-v13](GIT-PREFETCH-v13.md)。

## 代码负责人及 Issue 联动

见 [REVIEWS-v08.md](REVIEWS-v08.md)：目标快照 CODEOWNERS、分节和独立负责人门禁、默认分支自动关闭 Issue、持久化重试恢复与 R2 双路 I/O。完整 GitLab/Gogs 目标仍进行中。

## Issue 工作流

见 [ISSUES-v09.md](ISSUES-v09.md)：项目 Issue 筛选与游标、可保存的标签看板、事务批量操作、版本化编辑及评论分页。项目归档/转移和大仓库能力继续开发，不以本轮交付替代完整目标。

## 项目归档

- 所有者版本化归档/恢复；DO 串行 Git 写屏障、D1 协作写约束、原子 CI 租约取消及上游任务取消。读取、clone/fetch、导出、历史应用保留；权限管理与删除继续可用。
- 详细契约见 [ARCHIVE-v10](ARCHIVE-v10.md)，验收见 [VERIFICATION-v10](VERIFICATION-v10.md)。项目转移与大仓库流式处理仍在总目标内。

## 项目转移与重命名

- 转移保留项目 UUID、Git/R2 内容、协作与 CI 历史，重新计算空间继承权限；跨空间交接撤销连接并关闭公开应用，同空间重命名保留连接与发布。
- 历史地址重新授权，防止私有目的地泄露和旧地址占用；请求级 D1 版本事务与 DO 请求版本检查阻止旧空间在途写入。
- 详见 [TRANSFER-v11](TRANSFER-v11.md) 与 [VERIFICATION-v11](VERIFICATION-v11.md)。大仓库、完整流水线和统一登录等目标继续推进。

## Git 与云端工作流

- v0.12/v0.13 增加持久对象索引、增量协商、出站流式 pack、缓存及有界并发读取，见 [GIT-SCALE-v12](GIT-SCALE-v12.md) 和 [GIT-PREFETCH-v13](GIT-PREFETCH-v13.md)。入站规模限制和偶发写入失败仍须处理。
- v0.14 增加固定提交配置、依赖任务、并行执行、跨任务产物、整条工作流的取消和合并/发布门禁；实现及验收见 [CI-WORKFLOWS-v14](CI-WORKFLOWS-v14.md)。

## 后续目标与完成标准

v0.15 增加有界写入恢复和阶段诊断，并修正 thin pack 吞掉存储异常的问题，见 [GIT-RELIABILITY-v15](GIT-RELIABILITY-v15.md)。这些改动不等于已定位历史 503，可靠性目标仍需持续验证。

1. **Git 可靠性与大仓库**：原生 Git 持久审计和提交后审计故障隔离见 v0.29；继续定位历史 503/500 的具体根因；为入站 pack/delta 提供有界流式处理，扩展首次导入规模，并验证失败后引用不前移、重试不损坏对象、原生 clone/fetch/fsck 一致。
2. **完整 CI/CD**：定时流水线见 v0.19，项目变量与密钥见 v0.20；共享缓存见 v0.21；云端 TypeScript/npm 受限编译见 v0.22；空间变量继承见 v0.24。公共 npm 下载缓存见 v0.35；继续补齐更广泛前端工具链。任务必须绑定确定的代码和配置版本，权限撤销后停止读取凭据或发布，提供失败恢复和真实部署证据。
3. **身份与授权**：OIDC 统一登录与受控注册见 v0.23；细粒度部署令牌见 v0.26；GitHub/GitLab OAuth 见 v0.33；继续实现 Cloudflare Access、其他提供方和完整账户恢复，验证账户关联、角色变更及凭据撤销。
4. **日常协作**：跨项目协作搜索见 v0.28，合并队列见 v0.31；默认分支代码索引见 v0.32；v0.34 已实现索引内容跨代复用；继续补齐索引规模验证、Wiki 迁移和 PDF/Notebook 预览，保持私有项目隔离、可审计操作与可恢复数据。
5. **扩展平台能力**：npm/通用包仓库见 v0.25；继续补齐其余格式、镜像仓库、扫描及质量报告并独立验收；完整 GitLab YAML/API 兼容不在已有版本的完成声明中。

总目标持续有效。每一阶段须有真实云端持久化、权限/失败路径验证、可操作界面或明确的客户端契约、开源文档、发布和源码同步；阶段版本号不代表完整 GitLab/Gogs 目标已经完成。

空间级变量继承、项目覆盖和跨项目撤销在 v0.24 实现，见 [空间变量](CI-WORKSPACE-VARIABLES-v24.md)。包下载缓存、身份扩展、包仓库及其余协作能力继续推进。

## 流式首次导入

原生 HTTPS receive 使用 R2 会话隔离区增量解析，完整 pack 校验和 delta 解析后再保存正式对象并发布 DO 引用；引入可恢复分页清理。单次应用预算提升至 64 MiB 传输、25,000 条目、256 MiB 展开，单对象仍为 8 MiB。验收与其余路径边界见 [入站记录](GIT-RECEIVE-v16.md)。packed R2、读取并发、大型 Fork/历史算法和剩余平台功能继续推进。

本轮类型检查及 182 项单元测试、本地 43 次核心 API 断言和 35 项大包检查通过。生产单次首次推送 2,108 对象/35 MiB、v0/v2 clone/fsck、增量与中断下载专项通过 33 项检查，另有 57 次写入/CI 断言；夹具已清理。首次导入约 12 分钟，冷克隆仍需优化，完整平台目标不因此标记完成。

## 完整 pack 缓存

在权限和可达对象集合检查之后复用 R2 完整 pack，减少重复 clone 的对象读取与压缩；有界分块校验、损坏回退、24 小时保留、失败恢复和仓库删除回收见 [GIT-PACK-CACHE-v17](GIT-PACK-CACHE-v17.md)。首次导入、无缓存传输和同仓库串行排队仍属后续性能目标，永久对象存储格式保持兼容。

## 传输期间的页面读取

原生 Git 传输期间，常用页面可通过有界只读通道读取引用快照；原主队列、生命周期等待及缓存回收屏障保留。实现、资源预算和验证见 [GIT-READS-v18](GIT-READS-v18.md)。两个完整 Git 传输并行、首次导入吞吐和复杂历史算法仍待优化，完整平台目标持续有效。

## Cloudflare 内的 TypeScript/npm 构建

独立 Worker 执行 WASM 编译，按固定提交读取源码与 npm v2/v3 锁文件，强制 SHA-512 完整性验证；生成 JS/CSS、R2 产物和可回滚应用版本。支持范围、资源限额与真实本地/生产验收见 [云端构建](CI-BUILDS-v22.md)。不运行任意 npm 生命周期脚本；空间变量继承、统一登录、包仓库及其余协作/规模能力仍在总目标内。

## 定时流水线

持久化 Cron/IANA 时区计划、Queues 幂等派发、固定代码与配置快照、当前所有者权限、撤权与生命周期取消、配置界面及历史见 [CI-SCHEDULES-v19](CI-SCHEDULES-v19.md)。共享缓存、变量/密钥、云端工具链和完整平台目标继续推进。

## 项目 CI 变量与密钥

项目加密变量、环境覆盖、任务显式选择与加密快照、MR 来源限制、租约授权、撤权取消、日志脱敏和管理界面见 [CI-VARIABLES-v20](CI-VARIABLES-v20.md)。空间级继承、共享缓存、云端工具链和身份接入继续推进。

## 共享构建缓存

R2 缓存、锁文件键、分支/保护状态隔离、成功门禁、代号清空与回收队列见 [CI-CACHES-v21](CI-CACHES-v21.md)。云端工具链、空间变量继承、身份接入及完整平台目标继续推进。

## 统一登录

管理员管理提供方、标准 OIDC 代码流和 PKCE、账户显式关联、普通无密码账户创建、本地 MFA、配置变更撤销关联会话与 PAT，见 [OIDC-v23](OIDC-v23.md)。配置与登录状态由 Workers/D1 处理，不引入容器。实际商业提供方、普通 OAuth、Access、完整恢复和后续平台能力仍按各自验收标准推进。

## 项目包仓库

Workers 原生 npm 协议、通用文件上传、R2 不可变字节、D1 原子元数据和清理记录、当前用户/项目权限及网页管理，见 [包仓库](PACKAGES-v25.md)。支持真实 npm 发布、安装、标签和撤回；私有包的原生云构建接入、部署令牌、其余包格式、镜像仓库及完整 GitLab/Gogs 目标继续开发。

## 部署令牌

项目/空间独立主体、四项细粒度权限、Git/LFS 只读、npm/通用包自动化、有限有效期、原子轮换/撤销、跨空间转移撤权和网页管理见 [部署令牌](DEPLOY-TOKENS-v26.md)。全平台目标持续有效；下一阶段继续接入原生 Worker 私有包构建及后续身份、协作和扩展能力。

## 私有 npm 原生云构建

受控主 Worker 从 R2 读取明确授权的私有依赖，仅将校验后的包字节交给 WASM 编译 Worker。D1 持续约束依赖授权、产物/缓存/应用发布和整个工作流；显式变量、原生 npm 锁文件、使用方法与限额见 [私有构建](CI-PRIVATE-PACKAGES-v27.md)。包下载缓存、更广泛工具链、身份扩展、协作与完整平台目标继续推进。

## 自助密码找回

离线一次性密码恢复密钥、MFA 联合验证、原子密码/凭据/审计变更、轮换与撤销及页面见 [密码恢复](PASSWORD-RECOVERY-v30.md)。邮件验证与恢复、全部第二因子丢失后的身份核验、Access/普通 OAuth 等仍在完整平台目标中。

## 合并队列

DO 持久驱动每个目标分支的 FIFO 队列，在 R2 准备固定合并候选，使用目标配置进行 CI，并发布相同 SHA。目标变化使旧批准失效，源变化取消意向，失败保留队首等待处理；新增队列界面与受保护分支要求。详见 [合并队列](MERGE-QUEUE-v31.md)。整体平台目标继续进行，不将阶段版本等同于完整 GitLab/Gogs。

## 全局代码索引

Workers/DO 分批读取 R2 Git 快照，使用主 D1 普通倒排表、持久游标与原子代号发布，搜索直接校验当前权限。支持默认分支字面量、空间/路径/扩展名筛选、固定提交行跳转、覆盖状态和维护者重建；推送、同步、默认分支切换及定时补漏驱动更新。详见 [代码搜索](CODE-SEARCH-v32.md)。不宣称完整历史/多分支/正则索引或大型实例性能等价；整体 GitLab/Gogs 目标持续有效。

## OAuth 身份接入

新增 GitHub 与 GitLab 的授权码/PKCE、固定用户 API 和稳定身份 ID，复用本地关联/MFA/普通注册/撤权流程，管理员可在界面配置可信实例。GitHub 邮箱限制与 GitLab 已确认主邮箱策略、实际应用配置要求及验证范围见 [OAuth 登录](OAUTH-v33.md)。Access、目录同步、邮箱恢复、大仓库成本及完整平台目标仍持续推进。

## 代码索引内容复用

普通推送复用同仓库、同缓存周期、同 Git blob 的文本与倒排关系，重命名和复制无需重新读取和分词。路径映射仍按固定提交完整构建，手动重建采用新缓存周期，旧快照在发布前持续可用。迁移兼容旧索引读取，并提供复用/新增/写入计数。见 [实现与边界](CODE-CONTENT-v34.md) 和 [验收](VERIFICATION-v34.md)。全历史/多分支索引、树差异遍历和大规模成本验证仍未完成。

## 公共 npm 下载缓存

独立编译 Worker 使用专用 R2 桶复用锁定公共 tarball，命中仍校验 SHA-512，损坏/过期/缓存故障回退至 registry。私有包不入缓存，原有压缩/展开预算保留；构建日志包含命中、写入、异常及下载量。部署、过期和边界见 [缓存说明](CI-NPM-CACHE-v35.md)。原生 Git、大仓库、完整 CI 工具链及其他 GitLab/Gogs 差距继续推进。

## TypeScript 构建配置

Worker WASM 编译支持所选 tsconfig/JSONC、相对配置继承、baseUrl/paths、JSX 设置及项目源码扩展名替换；重复配置只解析一次。边界和配置见 [构建配置](CI-TSCONFIG-v36.md)。完整前端工具链、项目引用构建、类型检查和其他平台能力仍继续推进。

## Notebook 预览

补齐数据分析仓库的 `.ipynb` 浏览入口，提供只读单元/输出/附件、源码切换和安全降级。实现与限制见 [Notebook 预览](NOTEBOOK-v37.md)。Notebook 预览是该阶段开发范围的收尾。Notebook 预览不包含交互内核或任意 Python 运行时，后续功能作为独立需求规划。
