# v0.12 验证记录

**简体中文** · [English](en/VERIFICATION-v12.md)

2026-09-08，持久 Git 完整性索引、流式出站 pack、增量协商。

- TypeScript 与 140 个测试通过。新增覆盖索引重建、闭包写入事务失败、R2 缺失/伪造哈希、引用提交失败后的隐藏对象隔离、跨 UUID 索引拒绝、5,412 对象元数据遍历、大排除集合、pack 校验和、背压/取消/空闲超时排空，以及 v0/v2 不确认不可见 have。
- 本地真实 Git 大仓库验收 49 项通过：6 次分批 push，5,424 个对象与 42 MiB 随机文件内容；v0/v2 clone、后续新提交 push、v0/v2 增量 fetch、各克隆 `git fsck --full --strict`、匿名私有仓库拒绝、下载中断后继续 Git 访问。
- 本地 v2/v0 冷克隆约 2.88/2.81 秒；后续新提交 push 84 毫秒，两种协议增量 fetch 均约 103 毫秒。这些是本机 Workers/R2 模拟器结果，不能用作生产性能承诺。
- 下载统计显示全量约 44.4 MB R2 读取，读取缓存峰值 7,546,745 字节；增量协商修复后只涉及新增的 514 字节缓存对象，因共享缓存命中未再读取 R2。该指标仅为对象缓存，不代表整个 Worker isolate 的峰值内存。
- 本地项目转移回归 80 项、协作与隔离云 CI 82 项通过；计数包含状态轮询。认证头调整后，核心回归再通过 43 项 API 断言及原生 Git v0/v2、增量 thin push、并发引用更新、LFS、权限与 R2 持久化检查。
- 首次生产验收出现两次 API 提交 503 与一次原生推送持久化失败，原因尚未确定；失败轮次的临时项目已清理。随后独立冷启动 API/Git 提交成功，转移复验 83 项、协作与云 CI 80 项通过且临时项目已清理。这些复验前没有修改 Git 运行时代码，不能据此声称已定位或修复首次失败的根因。
- 大仓库生产复验通过 47 项检查：5,424 个对象、42 MiB 随机内容，原生 v0/v2 clone、增量 push/fetch、`git fsck --full --strict`、匿名拒绝、中断下载后恢复访问。v2/v0 克隆耗时 640,543/366,723 毫秒，增量 push 4,192 毫秒，v2/v0 fetch 5,519/3,496 毫秒。生产私有仓库传输完整性通过，但冷克隆性能仍须优化。
- 生产全量下载分别读取 R2 5,423/5,424 次、44,365,169/44,365,416 字节，对象缓存峰值均 7,546,745 字节；两次增量下载均命中新增的 514 字节缓存对象，没有额外 R2 读取。取消下载在已读取 79 个 R2 对象后停止，后续 Git 请求成功。此轮调试日志未捕获新的 Git 失败事件。
- 首次部署健康接口 0.12.0、13 个资源和源码哈希、旧站 检查通过。最终发布使用 `scripts/verify-release.mjs` 核对在线资源、源码包、版本及 API/Git 认证头，并执行独立原生 Git 拉取与 `fsck`；实际部署标识及源码 SHA 记录在发布清单中。
- 独立 Chromium headless 浏览器验收通过 43 项检查，临时仓库/空间已清理，测试用户已停用并撤销凭证。包含错误密码提示、登录、管理员创建用户、空间创建/切换/权限继承、项目创建、代码高亮与 HTML 转义、Issue/看板、隔离云 CI、归档恢复、重命名及旧 URL 跳转、账号与管理员页面、390 px 移动端、只读成员界面与写入 403、匿名私有仓库拒绝。
- UI 验收发现并修复移动端导航文字挤压、长代码撑宽页面；代码横向滚动限制在代码框中。另修复 API 的 401 错误发送 HTTP Basic challenge，导致浏览器等待认证而不交付 fetch 响应的问题。只为 Git/LFS URL 保留 Basic challenge；对应单元断言、错误密码及匿名页面浏览器回归通过。
- 另在 1,280×720 桌面视口验证侧栏可滚动，避免底部链接溢出背景；390 px 移动端再次验证无整页横向溢出。`npm run build:production` dry-run 通过。

剩余工作见 [v0.12 大仓库边界](GIT-SCALE-v12.md) 与 [持续能力矩阵](CLOUD-NATIVE-v05.md)，总目标保持进行中。
