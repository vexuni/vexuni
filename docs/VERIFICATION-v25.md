# v0.25 包仓库验收

**简体中文** · [English](en/VERIFICATION-v25.md)

日期：2026-09-09（Asia/Singapore）。契约与边界见 [包仓库](PACKAGES-v25.md)。生产候选提交 `0d3444b`，主 Worker `c3eceaf0-ff24-4cf2-ad68-fe3431c35e42`。后续发布仅补充此验证文档及对应源码归档，运行时代码保持一致。

| 环境         | 检查                                                     | 结果                                                                      |
| ------------ | -------------------------------------------------------- | ------------------------------------------------------------------------- |
| 类型与单元   | `npm run check`                                          | 280 项测试通过，包含 16 项新增包存储、npm 压缩包及路由测试                |
| 生产打包     | `npm run build:production`                               | Wrangler dry-run 通过，既有绑定保留                                       |
| 本地包仓库   | `PACKAGE_LARGE=1 npm run test:packages`，配置 Playwright | 60 项检查、48 次直接 API 请求通过；npm 内部 HTTP 请求不计入该数字         |
| 本地核心 Git | `npm run test:e2e`                                       | 43 次 API 断言及原生 Git、并发、LFS、持久化检查通过                       |
| 生产包仓库   | 独立临时项目与私有令牌文件                               | 56 项检查、46 次直接 API 请求通过，包含 15 MiB npm 内容和 64 MiB 通用文件 |
| 生产 Git/CI  | 原生 push → 固定提交工作流 → clone/严格 fsck             | 29 次检查通过，包含推送、版本化 DAG、克隆与严格 fsck                      |

本地验收使用真实 npm CLI，覆盖普通和作用域包名、两版本发布、安装分发标签、SHA-512 tarball 完整性、标签增加和移除、部分与全部撤回、陈旧 `_rev`、不可重用版本、私有下载拒绝。大包场景包含 15 MiB 随机二进制内容的 npm 包，以及 64 MiB 通用文件的流式上传、下载和完整性核对。

通用包覆盖 HEAD、字节区间和后缀 Range、416、ETag、SHA-256 不匹配、重复和并发发布、私有/公开切换、显式无效令牌、删除后不能覆盖。项目重命名保留 UUID 和文件，旧路径读取跳转、写入拒绝；归档后下载继续、发布停止。无头 Chromium 验证项目包仓库入口、文件选择、浏览器 SHA-256 上传、详情与撤回，且没有页面运行时异常；截图经人工视觉检查。本轮浏览器验收为桌面布局，不追加移动端覆盖声明。

新增单元测试覆盖：R2 完成前不可见、多个通用文件共享版本、上传中撤权/禁用/转移/归档、失败和迟到上传回收、数据库提交成功但响应丢失时保留已发布对象、项目级联删除后清理表仍存在、发布角色与写凭据、32 标签上限的原子回滚、当前授权下载和取消尚未读取的流。npm 解析覆盖实际包清单优先、二进制与捆绑文件、PAX/GNU 长路径、重复/越界/链接/截断/校验和错误及压缩炸弹。

生产迁移前导出 D1，在独立 SQLite 副本应用 `0020_packages.sql`；60 张既有表行数保持一致，数据库完整性和外键检查通过。生产迁移执行 17 条语句成功。此备份恢复验证只覆盖 D1，不等于 R2/DO 全站灾备演练。编译 Worker、应用网关、R2 桶、D1 身份和 Durable Object 绑定保留。

生产包与 Git/CI 验收都已完成清理。通过只读 D1 查询确认两类临时空间、项目，以及包版本和独立上传清理表的夹具记录均为零；客户端临时 npm/Git 目录及凭据文件已由验收程序移除。原始用户 PAT 不被验收程序撤销。

最终发布检查使用 `npm run verify:release` 对照线上静态模块、OpenAPI、源码归档及健康版本，并检查 example.com 旧站；源码推送后在独立 Git 镜像 fetch 并执行严格 fsck，对照发布提交。最终版本、提交和核对输出记录在发布工作区中。此次不声明完整 GitLab/Gogs 已完成；私有 registry 原生云构建接入、部署令牌、其他包格式与平台差距继续推进。
