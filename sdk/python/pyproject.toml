[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "vexuni"
version = "0.3.0"
description = "Async vexuni Git hosting client"
requires-python = ">=3.10"
license = "AGPL-3.0-only"
dependencies = ["cryptography>=46,<52"]
